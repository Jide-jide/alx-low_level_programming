#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _abs - Write a function that computes the absolute value of an integer.
 * @c : number to check
 * Return:0 or 1
 */

int _abs(int c)
{
	x = abs(c);
	_putchar(x);
	return (0);
}
